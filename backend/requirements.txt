# Production deps (Render / cloud). Local OCR/vision extras: requirements-local.txt
# Run: uvicorn main:app --reload

# FastAPI & Server
fastapi==0.115.2
uvicorn==0.30.6
python-multipart==0.0.9

# Gemini is called via HTTPS REST in services/gemini_rest.py

# Utility
requests==2.32.3

# MongoDB Driver (dnspython required for mongodb+srv:// URIs)
pymongo>=4.6.0
dnspython>=2.6.0

# Environment Variables
python-dotenv==1.0.1

# Authentication
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
# bcrypt 4.1+ removed __about__; passlib 1.7.4 still expects it (trapped error on hash/verify).
bcrypt==4.0.1
email-validator>=2.0.0

# PDF Generation
fpdf==1.7.2

# Payments (razorpay 1.4.x needs pkg_resources; pin setuptools before it removed the shim)
setuptools==69.5.1
razorpay==1.4.2
